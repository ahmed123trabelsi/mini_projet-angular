import { FilterByNumeroChambrePipe } from './filter-by-numero-chambre.pipe';

describe('FilterByNumeroChambrePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNumeroChambrePipe();
    expect(pipe).toBeTruthy();
  });
});
